import type { Band, CreditGate } from "@vivaha/shared";
export interface ItemView { id: string; sku: string; designNo: string | null; name: string; nameHi: string; lineId: string; attrs: Record<string, string>; uom: string; packUom: string; perPack: number; moq: number; landedCost: number; hsn: string; gstPct: number; vendorId: string | null; status: string; season: string | null; batchTracked: boolean; wastagePct: number | null; setupCharge: number | null; artSeed: number; imageUrl: string | null; slabs: { fromQty: number; toQty: number; rate: number }[]; onHand: number; reserved: number; hold: number; damaged: number; quarantined: number; available: number; godowns: { godownId: string; onHand: number; reserved: number; hold: number; damaged: number; quarantined: number; available: number }[]; band: Band; inTransitEta: string | null }
export interface Customer { id: string; name: string; contactName: string; group: string; tehsil: string; creditLimit: number; creditDays: number; gateMode: "WARN" | "BLOCK"; gstin: string | null; firmType: string; phone: string; address: string; linesEnabled: string[]; referCode: string; salesExec?: { id: string; name: string } | null; salesExecId: string | null; status: string; blockReason: string | null; blockedBy: string | null; blockedAt: string | null; blockUntil: string | null; contacts: { id: string; name: string; role: string; phone: string; hasLogin: boolean; authority: string }[]; machines: { id: string; type: string; spec: Record<string, string> }[]; gate: CreditGate; ageing: number[]; hasLogin?: boolean }
export interface OrderLine { id: string; itemId: string; lineId: string; qty: number; rate: number; slabRate: number; mult: number; priceSrc: string; amount: number; gstPct: number; hsn: string; alloc: Record<string, number>; shipped: number; item: { id: string; sku: string; name: string; nameHi: string; uom: string; designNo: string | null; artSeed: number; lineId: string; imageUrl: string | null } }
export interface Order { id: string; customerId: string; status: string; subtotal: number; tax: number; total: number; requiredBy: string; holdUntil: string | null; approvedAt: string | null; bookedBy: string; source: string; createdAt: string; customer: { id: string; name: string; contactName: string; tehsil: string; group: string; gstin: string | null; address: string; creditLimit: number; creditDays: number; gateMode: "WARN" | "BLOCK"; blockReason: string | null }; lines: OrderLine[]; events: { id: string; from: string | null; to: string; by: string; why: string; at: string }[]; dispatches: { id: string; transporter: string; lr: string; tracking: string; packages: number; freight: number; ewb: string | null; invoiceNo: string | null; at: string }[]; invoices: Invoice[]; backorder: number; gate: CreditGate }
export interface Invoice { no: string; orderId: string; customerId: string; date: string; taxable: number; cgst: number; sgst: number; igst: number; total: number; blocks: { gstPct: number; taxable: number; inter: boolean; igst: number; cgst: number; sgst: number }[]; status: string; lines: { id: string; itemId: string; itemName: string; sku: string; hsn: string; qty: number; rate: number; amount: number; gstPct: number }[] }
